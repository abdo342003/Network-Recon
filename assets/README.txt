Place your application branding assets here.

- `app.ico`         -> Windows ICO file used as the executable and installer icon.
- `NetworkRecon.png` -> Application screenshot (used in README).
- `logo.png`        -> Optional PNG used for installer banners or marketing.

Create or copy your icon file as `app.ico` into this folder before running the build scripts.
